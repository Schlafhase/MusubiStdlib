; Arithmetics for binary numbers encoded using
; scott lists of booleans
#once
#include stdlib/boolean.mbim

let
  Sb := rec (\Sb.\n -> 
         n [ true ] (\x.\xs ->
           if x then
             cons false (Sb xs)
           else
             cons true xs
           end
       ),
in
