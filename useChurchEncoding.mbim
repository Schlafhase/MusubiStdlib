; Adds an alias without the 's' suffix to all
; defined functions for church numerals
#once
#include stdlib/churchArithmetics.mbim

let
  zero := 0c,
  S := Sc,
  pred := predc,
  isZero := isZeroc,
  add := addc,
  sub := subc,
  mult := multc,
in

