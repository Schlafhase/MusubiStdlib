; Utilities for creating lazy lambdas
#once

let
  ; Wraps a function with no arguments to delay evaluation
  #define lazy \_.
  ; Evaluates lazy functions created using the lazy macro
  ; Parameter f:
  ;  The lazy lambda to evaluate
  ; Returns:
  ;  What was wrapped by `f`
  force := \f.f (\x.x)
in
