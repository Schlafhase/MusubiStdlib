; Utilities for creating lazy lambdas
#once

let
  ; wraps a function with no arguments to delay evaluation
  #define lazy \_.
  ; for evaluating lazy functions created using lazyNoArg
  force := \f.f (\x.x)
in
